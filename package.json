{
  "name": "layerup-weather",
  "version": "1.0.0",
  "private": true,
  "description": "Compare today and yesterday with transparent, keyless weather data",
  "scripts": { "check": "node --check app.js", "test": "node --test tests/*.test.cjs" },
  "engines": { "node": ">=22" }
}
